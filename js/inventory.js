class inventory {
  constructor(image,hp,speed,firePower,fireSpeed) {
    this.texture = image;
    this.hp = hp;
    this.speed = speed;
    this.firePower = firePower
    this.fireSpeed = fireSpeed
  }
}
